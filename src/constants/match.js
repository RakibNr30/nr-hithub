const STAGE = Object.freeze({
    UPCOMING: 0,
    TOSS: 1,
    IN_PROGRESS: 2,
    END: 3,
});

const TOSS_DECISION = Object.freeze({
    BAT: "bat",
    BOWL: "bowl",
});

export {STAGE, TOSS_DECISION}